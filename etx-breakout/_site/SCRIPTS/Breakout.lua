-- etx-breakout
local function a(self)local b={prototype={}}b.prototype.__index=b.prototype;b.prototype.constructor=b;return b end;local function c(d,...)local e=setmetatable({},d.prototype)e:____constructor(...)return e end;local f={}local g=a()g.name="Game"function g.prototype.____constructor(self,h,i)self.w=h;self.h=i;self.paddleX=0;self.paddleY=0;self.paddleW=0;self.paddleH=0;self.ballX=0;self.ballY=0;self.ballVx=0;self.ballVy=0;self.ballR=6;self.ballStuck=true;self.bricks={}self.bricksRemaining=0;self.score=0;self.best=0;self.lives=3;self.state={initial=0,playing=1,paused=2,gameOver=3,win=4}self.phase=self.state.initial;self.lastTick=0;self.soundEnabled=true;self:computeLayout()self.lastTick=getTime()end;function g.prototype.computeLayout(self)self.paddleW=math.max(58,math.floor(self.w*0.16))self.paddleH=math.max(8,math.floor(self.h*0.025))self.paddleY=self.h-math.max(16,math.floor(self.h*0.06))self.ballR=math.max(5,math.floor(self.w*0.011))self.paddleX=math.floor(self.w/2)end;function g.prototype.playSfx(self,j,k,l)if l==nil then l=0 end;if not self.soundEnabled then return end;playTone(j,k,l)end;function g.prototype.clamp(self,m,n,o)return math.max(n,math.min(o,m))end;function g.prototype.isTouchEvent(self,p)return p==EVT_TOUCH_FIRST or p==EVT_TOUCH_SLIDE or p==EVT_TOUCH_TAP or p==EVT_TOUCH_BREAK end;function g.prototype.readTouchPosition(self)if type(getLastPos)~="function"then return nil end;local q,r=getLastPos()if type(q)=="number"and type(r)=="number"then return{x=q,y=r}end;if type(q)=="table"then local s=q;local t=s.x or s[1]local u=s.y or s[2]if type(t)=="number"and type(u)=="number"then return{x=t,y=u}end end;return nil end;function g.prototype.buildLevel(self)self.bricks={}local v=6;local w=10;local x=3;local y=math.floor(self.w*0.04)local z=math.max(22,math.floor(self.h*0.08))local A=math.floor((self.w-y*2-x*(w-1))/w)local B=math.max(10,math.floor(self.h*0.045))do local C=0;while C<v do do local b=0;while b<w do local D=y+b*(A+x)local E=z+C*(B+x)local F=C<2 and 2 or 1;local G=F==2 and COLOR_THEME_WARNING or COLOR_THEME_SECONDARY2;local H=self.bricks;H[#H+1]={x=D,y=E,w=A,h=B,hp=F,color=G}b=b+1 end end;C=C+1 end end;self.bricksRemaining=#self.bricks end;function g.prototype.resetBall(self,I)self.ballStuck=I;self.ballX=self.paddleX;self.ballY=self.paddleY-self.paddleH-self.ballR-1;self.ballVx=0;self.ballVy=0 end;function g.prototype.launchBall(self)if not self.ballStuck or self.phase~=self.state.playing then return end;self.ballStuck=false;self.ballVx=1.7;self.ballVy=-2.7;self:playSfx(880,60,0)end;function g.prototype.restart(self)self:computeLayout()self.score=0;self.lives=3;self:buildLevel()self:resetBall(true)self.phase=self.state.playing;self.lastTick=getTime()self:playSfx(860,90,0)end;function g.prototype.updatePaddle(self)local J=getValue("ail")/1024;local K=math.max(4,math.floor(self.w*0.02))self.paddleX=self.paddleX+J*K;self.paddleX=self:clamp(self.paddleX,self.paddleW/2,self.w-self.paddleW/2)if self.ballStuck then self.ballX=self.paddleX;self.ballY=self.paddleY-self.paddleH-self.ballR-1 end end;function g.prototype.handlePaddleCollision(self,L)if self.ballVy<=0 then return end;local M=self.paddleX-self.paddleW/2;local N=self.paddleX+self.paddleW/2;local O=self.paddleY-self.paddleH/2;local P=L+self.ballR<=O and self.ballY+self.ballR>=O;local Q=self.ballX>=M-self.ballR and self.ballX<=N+self.ballR;if not P or not Q then return end;local R=(self.ballX-self.paddleX)/(self.paddleW/2)self.ballVy=-math.abs(self.ballVy)self.ballVx=R*3.4;self.ballY=O-self.ballR-1;self:playSfx(640,20,0)end;function g.prototype.hitBrick(self,S)S.hp=S.hp-1;if S.hp<=0 then self.bricksRemaining=self.bricksRemaining-1;self.score=self.score+10;self:playSfx(520,35,0)else S.color=COLOR_THEME_SECONDARY1;self.score=self.score+4;self:playSfx(760,18,0)end;if self.bricksRemaining<=0 then self.best=math.max(self.best,self.score)self.phase=self.state.win;self:playSfx(980,130,0)end end;function g.prototype.handleBrickCollision(self,T,L)do local U=0;while U<#self.bricks do do local S=self.bricks[U+1]if S.hp<=0 then goto V end;local W=self:clamp(self.ballX,S.x,S.x+S.w)local X=self:clamp(self.ballY,S.y,S.y+S.h)local Y=self.ballX-W;local Z=self.ballY-X;if Y*Y+Z*Z>self.ballR*self.ballR then goto V end;self:hitBrick(S)local _=T<=S.x-self.ballR;local a0=T>=S.x+S.w+self.ballR;local a1=L<=S.y-self.ballR;local a2=L>=S.y+S.h+self.ballR;if _ or a0 then self.ballVx=-self.ballVx end;if a1 or a2 or not _ and not a0 then self.ballVy=-self.ballVy end;return true end::V::U=U+1 end end;return false end;function g.prototype.updateBall(self)if self.ballStuck or self.phase~=self.state.playing then return end;local T=self.ballX;local L=self.ballY;self.ballX=self.ballX+self.ballVx;self.ballY=self.ballY+self.ballVy;if self.ballX-self.ballR<=0 then self.ballX=self.ballR;self.ballVx=math.abs(self.ballVx)self:playSfx(420,12,0)elseif self.ballX+self.ballR>=self.w then self.ballX=self.w-self.ballR;self.ballVx=-math.abs(self.ballVx)self:playSfx(420,12,0)end;if self.ballY-self.ballR<=0 then self.ballY=self.ballR;self.ballVy=math.abs(self.ballVy)self:playSfx(420,12,0)end;self:handlePaddleCollision(L)self:handleBrickCollision(T,L)if self.ballY-self.ballR>self.h then self.lives=self.lives-1;self:playSfx(180,180,0)if self.lives<=0 then self.best=math.max(self.best,self.score)self.phase=self.state.gameOver else self:resetBall(true)end end end;function g.prototype.onEvent(self,p)if p==EVT_SYS_BREAK then self:restart()return end;if p==EVT_MODEL_FIRST then if self.phase==self.state.playing then self.phase=self.state.paused;self:playSfx(420,60,0)elseif self.phase==self.state.paused then self.phase=self.state.playing;self:playSfx(820,40,0)end;return end;if p==EVT_TELEM_FIRST or p==EVT_VIRTUAL_ENTER or p==EVT_ENTER_BREAK then self:launchBall()return end;if not self:isTouchEvent(p)then return end;if self.phase==self.state.initial or self.phase==self.state.gameOver or self.phase==self.state.win then if p==EVT_TOUCH_TAP or p==EVT_TOUCH_FIRST then self:restart()end;return end;local a3=self:readTouchPosition()if a3 then self.paddleX=self:clamp(a3.x,self.paddleW/2,self.w-self.paddleW/2)end;if p==EVT_TOUCH_TAP or p==EVT_TOUCH_FIRST then self:launchBall()end end;function g.prototype.drawBricks(self)do local U=0;while U<#self.bricks do do local S=self.bricks[U+1]if S.hp<=0 then goto a4 end;lcd.drawFilledRectangle(S.x,S.y,S.w,S.h,S.color)lcd.drawRectangle(S.x,S.y,S.w,S.h,COLOR_THEME_PRIMARY1)end::a4::U=U+1 end end end;function g.prototype.drawPaddle(self)local D=self.paddleX-self.paddleW/2;local E=self.paddleY-self.paddleH/2;lcd.drawFilledRectangle(D,E,self.paddleW,self.paddleH,COLOR_THEME_SECONDARY2)lcd.drawRectangle(D,E,self.paddleW,self.paddleH,COLOR_THEME_PRIMARY1)end;function g.prototype.drawBall(self)local a5=self.ballR*2;lcd.drawFilledRectangle(self.ballX-self.ballR,self.ballY-self.ballR,a5,a5,COLOR_THEME_WARNING)end;function g.prototype.drawHud(self)lcd.drawText(4,3,"S:"..tostring(self.score),bit32.bor(SMLSIZE,COLOR_THEME_PRIMARY1))lcd.drawText(56,3,"L:"..tostring(self.lives),bit32.bor(SMLSIZE,COLOR_THEME_PRIMARY1))lcd.drawText(self.w-4,3,"Best:"..tostring(self.best),bit32.bor(bit32.bor(SMLSIZE,RIGHT),COLOR_THEME_PRIMARY1))if self.ballStuck and self.phase==self.state.playing then lcd.drawText(self.w/2,self.h-14,"Tap/TELEM to launch",bit32.bor(bit32.bor(SMLSIZE,CENTER),COLOR_THEME_PRIMARY1))end end;function g.prototype.drawOverlay(self,a6)lcd.drawFilledRectangle(20,20,self.w-40,self.h-40,COLOR_THEME_SECONDARY1,1)lcd.drawRectangle(20,20,self.w-40,self.h-40,COLOR_THEME_PRIMARY1,2)lcd.drawText(self.w/2,self.h/2,a6,bit32.bor(bit32.bor(bit32.bor(COLOR_THEME_PRIMARY1,CENTER),VCENTER),DBLSIZE))end;function g.prototype.draw(self)lcd.clear(COLOR_THEME_PRIMARY2)self:drawBricks()if self.phase~=self.state.initial then self:drawPaddle()self:drawBall()end;self:drawHud()if self.phase==self.state.initial then self:drawOverlay("BREAKOUT\nPress SYS")elseif self.phase==self.state.paused then self:drawOverlay("PAUSED")elseif self.phase==self.state.gameOver then self:drawOverlay("GAME OVER\nPress SYS")elseif self.phase==self.state.win then self:drawOverlay("YOU WIN\nPress SYS")end end;function g.prototype.run(self,p,a7)if p~=nil then self:onEvent(p)end;local a8=getTime()if a8>self.lastTick then self:updatePaddle()self:updateBall()self.lastTick=a8 end;self:draw()return 0 end;local a9;local function aa(h,i)if h==nil then h=LCD_W end;if i==nil then i=LCD_H end;a9=c(g,h,i)end;local function ab(p,a7)return a9:run(p,a7)end;f.init=aa;f.run=ab;return f